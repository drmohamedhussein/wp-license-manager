/**
 * WPLM Client-Side Admin Script for Injected Licensed Products.
 *
 * This script handles the AJAX requests for license activation and deactivation
 * on the client plugin/theme's license settings page.
 */
jQuery(document).ready(function($) {
    // This script is primarily responsible for ensuring jQuery is loaded and
    // global AJAX setup if needed. The actual form submission logic is inlined
    // in the render_license_page() function for simplicity and to ensure it's
    // always specific to the injected plugin/theme's slug and nonces.
    
    // Any global client-side logic for ALL WPLM licensed products can go here.
    // For now, it's minimal as most logic is in the injected PHP template script tag.
});
